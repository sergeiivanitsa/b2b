services:
  gateway_api:
    build:
      context: .
      dockerfile: services/gateway_api/Dockerfile
    restart: unless-stopped
    ports:
      - "127.0.0.1:8001:8001"
    env_file:
      - .env.gateway
