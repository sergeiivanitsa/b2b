services:
  gateway_api:
    image: b2b-gateway-api:${GATEWAY_IMAGE_TAG:?exact release SHA image tag is required}
    restart: unless-stopped
    ports:
      - "127.0.0.1:8001:8001"
    env_file:
      - ${GATEWAY_ENV_FILE:-.env.gateway}
    environment:
      - GATEWAY_RELEASE_COMMIT
